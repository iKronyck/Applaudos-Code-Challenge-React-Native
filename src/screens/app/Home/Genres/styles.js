import {StyleSheet} from 'react-native';
import {resize} from '../../../../utils/styles';
import colors from '../../../../utils/colors';

export default StyleSheet.create({
  container: {paddingTop: resize(10, 'h'), paddingBottom: resize(10, 'h')},
});
